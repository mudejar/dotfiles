- clean: ['~']

- defaults:
    link:
      relink: true

- link:
    ~/.Xresources: 
      if: '[ `uname` = Linux ] && [ `uname -n` != pop-os ]' 
      path: linux/Xresources
    ~/.xprofile:
      if: '[ `uname` = Linux ] && [ `uname -n` != pop-os ]' 
      path: linux/xprofile
    ~/.bashrc:
      if: '[ `uname` = Linux ] && [ `uname -n` != pop-os ]' 
      path: linux/bashrc
    ~/.config/i3:
      if: '[ `uname` = Linux ] && [ `uname -n` != pop-os ]' 
      path: config/i3/
      create: true
    ~/.config/nvim:
      path: config/nvim/
      create: true
    ~/.config/terminator:
      path: config/terminator/
      create: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
