- clean: ['~']

- defaults:
    link:
      relink: true

- link:
    ~/.config/nvim:
      path: config/nvim/
      create: true
    ~/.config/terminator:
      path: config/terminator/
      create: true
    ~/.config/zsh:
      path: config/zsh
      create: true
    ~/.zshrc:
      path: zshrc
